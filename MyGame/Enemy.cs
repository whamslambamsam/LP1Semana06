using System;

namespace MyGame
{
    public class Enemy
    {
        public Enemy(string name)
        {
            this name = name; // name oculta this.name
            int health = 100;
            int shield = 0;
        }
    }
}